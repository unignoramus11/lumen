# Environment variables for Lumen

# Admin password for publishing content.
ADMIN_PASSWORD=your_admin_password

# The URL of your Next.js application. Used for API calls.
# In development, this is typically http://localhost:3000.
# In production, this should be your deployed domain (e.g., https://lumen-sigma.vercel.app).
NEXTAUTH_URL=http://localhost:3000

# A secret used by NextAuth.js for signing and encrypting tokens.
# Generate a strong, random string for this.
NEXTAUTH_SECRET=your_nextauth_secret

# MongoDB connection URI.
MONGODB_URI=your_mongodb_connection_string

# Public base URL for the application.
# This is often the same as NEXTAUTH_URL but can be used for client-side references.
NEXT_PUBLIC_BASE_URL=http://localhost:3000